<directive name="catch" type="resource-class">
  <param name="status">The status code to match.</param>
  <content type="resource-class">
    Contents dispatched to if the status code matches.
  </content>
  <desc>
    Matches the status code of the transaction with the statuscode parameter.
    If they match, the transaction dispatches to the subcontent.
    Otherwise, the transactions is returned as is. 
  </desc>
  <status code="ANY">
    If the status matches the status of the transaction,
    then any status code set by subcontent will be the on returned.
  </status>
</directive>

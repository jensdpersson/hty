-module(hty_cond_resource).

-export([mount/1]).

mount(Fspath) ->
  notyet.
  %case lists:reverse(Fspath:parts()) of
  %  ["cond", Expression] ->
  %    Conds = 'parse(Expression);
  %  ["cond"] ->
  %    Conds = no
  %e%nd,
  %apa = lim.
